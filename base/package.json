{
  "name": "mangobase",
  "version": "0.0.6",
  "description": "Low-code backend framework",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && node ./build.mjs && tsc -p tsconfig.build.json && yarn copy-admin",
    "clean": "rm -rf dist",
    "copy-admin": "cp -R ../admin/dist dist/admin",
    "prepare": "yarn build",
    "test": "vitest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^9.0.2",
    "@vitest/coverage-c8": "^0.31.0",
    "@vitest/ui": "^0.31.0",
    "esbuild": "^0.17.19",
    "eslint-config-base": "*",
    "typescript": "^5.0.4",
    "vitest": "^0.31.1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.1",
    "radix3": "^1.0.1"
  }
}
