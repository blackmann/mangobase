{
  "name": "mangobase-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:base": "yarn workspace mangobase run build",
    "build:express-server": "yarn workspace mangobase-express-server run build",
    "build:mongo-db": "yarn workspace mangobase-mongodb run build",
    "build": "yarn build:base && yarn build:express-server && yarn build:mongo-db",
    "test:base": "yarn workspace mangobase run test",
    "test:mongodb": "yarn workspace mangobase-mongodb run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "base",
    "eslint-config-base",
    "express-server",
    "mongo-db",
    "examples/*"
  ],
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
